# 泡泡大师（PopIt）项目详细文档

## 一、项目概述

**项目名称**：泡泡大师（PopIt）

**项目类型**：微信小游戏（WeChat Mini Game）

**技术栈**：
- 语言：JavaScript（ES6 Modules）
- 框架：微信小游戏原生开发（无第三方游戏引擎）
- 渲染：Canvas 2D（纯手工绘制，无 DOM）
- 后端：微信云开发（CloudBase），云函数使用 Node.js
- 数据库：微信云数据库（MongoDB 风格的文档数据库）
- AppID：`wxbd18a37b0c266cd0`
- 云环境：`cloud1-d2gbhgc8abb1ab532`
- 基础库版本：`3.16.1`

**核心玩法**：这是一款记忆力测试游戏。屏幕上显示一个泡泡网格（4×4 到 7×7），部分泡泡会闪烁高亮，玩家需要在观察阶段记住这些泡泡的位置，然后在游戏阶段凭记忆点击正确的位置。

---

## 二、目录结构

```
popit/
├── game.js                          # 游戏入口文件
├── game.json                        # 游戏配置（竖屏、网络超时等）
├── project.config.json              # 微信开发者工具项目配置
├── project.private.config.json      # 私有配置
├── src/
│   ├── Main.js                      # 游戏主类，整合所有模块
│   ├── GameState.js                 # 游戏状态管理（核心数据层）
│   ├── BubbleGrid.js                # 泡泡网格渲染器（Canvas 绘制）
│   ├── UIManager.js                 # UI 管理器（所有界面绘制与交互）
│   ├── AudioManager.js              # 音效管理器（Web Audio API 合成）
│   ├── WechatAPI.js                 # 微信 API 封装（云函数调用、分享等）
│   ├── config.js                    # 游戏配置常量
│   ├── utils.js                     # 工具函数集合
│   ├── seasonUtils.js               # 赛季周期计算工具
│   ├── polyfill.js                  # iOS 兼容性补丁
│   ├── Icons.js                     # Canvas 图标绘制工具
│   └── assets/images/               # 图片资源
│       ├── pop_logo.png             # 游戏 Logo
│       └── temp-avatar.png          # 默认头像
└── cloudfunctions/                  # 云函数目录
    ├── checkin/                     # 签到云函数
    ├── saveGameData/                # 保存游戏数据
    ├── loadGameData/                # 加载游戏数据
    ├── getLeaderboard/              # 获取总排行榜
    ├── getSeasonLeaderboard/        # 获取赛季排行榜
    ├── settleSeason/                # 赛季结算归档
    └── getSeasonArchive/            # 查询历史赛季归档
```

---

## 三、核心模块说明

### 3.1 游戏主类 Main.js

- 游戏的中央控制器，协调所有子系统
- 管理游戏循环（`gameLoop` -> `update` -> `render`），使用 `requestAnimationFrame`
- 处理所有触摸事件分发（根据当前 `phase` 和 `currentScreen` 路由到不同处理器）
- 管理生命周期事件（`wx.onHide`、`wx.onShow`）
- 支持多点触控（游戏阶段最多 2 个触摸点）

### 3.2 游戏状态 GameState.js

- 核心数据层，管理所有游戏状态
- 本地存储（`wx.setStorageSync`）与云端同步
- 数据合并策略：高分/高关卡取较大值，金币取较大值，签到以云端为准
- Storage 写入优化：批量延迟写入（16ms 合并），减少 IO 阻塞

### 3.3 泡泡网格 BubbleGrid.js

- 支持 4×4 到 7×7 动态网格
- 大量使用离屏 Canvas 缓存优化（背景、玻璃框单元格、整个玻璃框网格）
- 每个泡泡有独立配色（16 个预设 + 动态生成额外颜色）
- 泡泡状态：`normal`（暗色）、`pink`/`purple`/`blue`（激活发光）、`red`（错误）
- 观察阶段泡泡会闪动（缩放动画），点击后固定 1.04 倍缩放

### 3.4 UI 管理器 UIManager.js

- 所有 UI 界面的绘制和触摸交互
- 管理的界面：`menu`（主菜单）、`game`（游戏）、`win`（胜利）、`fail`（失败）、`pause`（暂停）、`leaderboard`（排行榜）、`season_leaderboard`（赛季排名）、`checkin`（签到）、`share`（分享礼包）
- 大量使用预创建渐变缓存、文字测量缓存、数值宽度预计算等优化
- 支持头像图片加载和缓存（最多 20 个）

### 3.5 音效管理器 AudioManager.js

- 基于 Web Audio API 合成音效（无音频文件）
- 4 种音效：`pop`（泡泡爆破）、`wrong`（错误）、`success`（成功）、`click`（点击）
- GainNode 对象池复用，减少 GC 压力
- 兼容微信小游戏环境（`wx.createWebAudioContext`）

---

## 四、游戏核心玩法

### 4.1 游戏流程

```
主菜单（MENU）
    ↓ 点击"开始游戏"
观察阶段（OBSERVE）：目标泡泡闪烁高亮，玩家记忆位置
    ↓
游戏阶段（PLAY）：泡泡恢复暗色，玩家凭记忆点击
    ↓
成功（WIN）或 失败（FAIL）
    ↓
循环
```

### 4.2 难度递进系统

#### 网格大小

| 关卡范围 | 网格大小 | 格子数 |
|---------|---------|--------|
| 1-19 关 | 4×4 | 16 格 |
| 20-39 关 | 5×5 | 25 格 |
| 40-59 关 | 6×6 | 36 格 |
| 60+ 关 | 7×7 | 49 格 |

#### 目标数量

| 关卡范围 | 起始目标数 | 增长规则 | 上限 |
|---------|-----------|---------|------|
| 1-19 关 | 2 个 | 每 3 关 +1 | 7 个 |
| 20-39 关 | 7 个 | 每 5 关 +1 | 9 个 |
| 40-59 关 | 9 个 | 每 5 关 +1 | 11 个 |
| 60+ 关 | 11 个 | 每 5 关 +1 | 13 个 |

#### 观察时间（递减）

| 关卡范围 | 起始时间 | 递减规则 | 下限 |
|---------|---------|---------|------|
| 1-19 关 | 2500ms | 每关 -60ms | 1500ms |
| 20-39 关 | 2200ms | 每关 -40ms | 1400ms |
| 40-59 关 | 2000ms | 每关 -30ms | 1400ms |
| 60+ 关 | 1800ms | 每关 -20ms | 1400ms |

#### 游戏时间（递减）

| 关卡范围 | 起始时间 | 递减规则 | 下限 |
|---------|---------|---------|------|
| 1-19 关 | 5000ms | 每关 -100ms | 3200ms |
| 20-39 关 | 4800ms | 每关 -70ms | 3400ms |
| 40-59 关 | 4500ms | 每关 -50ms | 3500ms |
| 60+ 关 | 4200ms | 每关 -30ms | 3600ms |

### 4.3 计分与奖励系统

**得分规则**：
- 每个正确点击的泡泡：+5 分

**通关奖励（金币）**：

| 关卡范围 | 通关奖励 |
|---------|---------|
| 1-19 关 | +5 金币 |
| 20-39 关 | +15 金币 |
| 40-59 关 | +30 金币 |
| 60+ 关 | +50 金币 |

**生命系统**：
- 初始 5 条生命，最大 5 条
- 超时（未点完所有目标）：-1 生命
- 点错不扣生命，仅提示"点错了！"
- 连续胜利每 5 关恢复 1 生命

**胜利弹窗触发条件**：
- 破历史最高关卡（本局只弹一次，显示"新纪录!"）
- 固定关卡：第 5、10、20、30、40 关
- 40 关后每 10 关

---

## 五、功能模块详解

### 5.1 签到系统

#### 概述

签到系统是玩家获取金币的核心途径之一，采用连续签到递增奖励的模式，鼓励玩家每日回访。

#### 前端实现

- 本地快速校验 + 云端优先策略
- 签到以云端数据为准（防篡改）
- 云端不可用时降级到本地签到

#### 后端实现（checkin 云函数）

- 服务端校验日期、计算连续天数、发放奖励
- 使用东八区时间（UTC+8）

#### 奖励规则

| 签到天数 | 奖励金币 | 说明 |
|---------|---------|------|
| 第 1 天 | 300 金币 | 基础奖励 |
| 第 2 天 | 500 金币 | 递增奖励 |
| 第 3 天 | 1000 金币 | 递增奖励 |
| 第 4 天 | 1000 金币 | 递增奖励 |
| 第 5 天 | 1000 金币 | 递增奖励 |
| 第 6 天 | 1000 金币 | 递增奖励 |
| 第 7 天 | 3000 金币 | 含 bonus 大奖 |
| 7 的倍数天 | 额外 +2000 金币 | 连签 bonus |

- 签到天数持续累加，每 7 天为一个周期显示

#### UI 表现

- 8 个格子（6 天 + 第 7 天 + 连签奖励卡片）
- 显示当前周期的签到进度
- 已签到日期高亮标记

#### 防篡改机制

1. 前端先做本地快速校验（今天是否已签）
2. 调用 `checkin` 云函数
3. 服务端校验日期、计算连续天数、计算奖励、更新金币
4. 返回结果更新本地状态
5. 云端不可用时降级到本地签到

---

### 5.2 分享系统

#### 概述

分享系统提供两种分享模式，用于激励玩家通过分享获取金币，同时实现游戏传播。

#### 快速分享（底部分享按钮）

| 项目 | 说明 |
|------|------|
| 触发方式 | 点击主菜单底部分享按钮 |
| 奖励 | 每次 +50 金币 |
| 每日上限 | 10 次 |
| 实现 | 调用 `wx.shareAppMessage` 后立即发奖 |

#### 分享礼包（右上角宝箱图标）

| 项目 | 说明 |
|------|------|
| 触发方式 | 点击右上角宝箱图标 |
| 奖励 | 每次 +500 金币 |
| 每日上限 | 1 次 |
| 冷却机制 | 3 秒冷却防刷 |
| UI | 弹出分享礼包弹窗，显示微信消息预览 |

#### 分享配置

- 标题："来挑战泡泡大师！"
- 携带参数：`score`（分数）和 `wave`（关卡）

---

### 5.3 复活/续命系统

#### 概述

当玩家游戏失败（生命耗尽）时，提供两种复活方式，让玩家有机会继续当前关卡而不必返回首页。

#### 方式一：金币购买生命

| 项目 | 说明 |
|------|------|
| 每局上限 | 最多购买 3 次 |
| 价格递增 | 第 1 次 300 金币 → 第 2 次 500 金币 → 第 3 次 1000 金币 |
| 效果 | 生命 +1，重新开始当前关卡 |

#### 方式二：分享复活

| 项目 | 说明 |
|------|------|
| 每局上限 | 最多 3 次 |
| 触发方式 | 触发微信分享后返回游戏即生效 |
| 效果 | 生命 +1，重新开始当前关卡 |
| 超时保护 | 10 秒超时保护（如果 `onShow` 未触发则自动清除标志） |

#### 失败弹窗按钮逻辑

| 场景 | 按钮展示 |
|------|---------|
| 两种复活方式都可用 | 并排显示"金币购买"和"分享复活" |
| 还有复活方式可用 | 只显示"返回首页" |
| 两种方式都用完 | 并排显示"重新开始"和"返回首页" |

---

### 5.4 排行榜系统

#### 总排行榜

| 项目 | 说明 |
|------|------|
| 云函数 | `getLeaderboard` |
| 排名维度 | 按"最高分"和"最高关卡"切换 |
| 数据范围 | Top 50（最多 100） |
| 缓存策略 | 本地缓存 30 分钟（1800000ms） |
| 展示内容 | 包含当前用户排名 |

#### 赛季排行榜

| 项目 | 说明 |
|------|------|
| 云函数 | `getSeasonLeaderboard` |
| 数据集合 | `seasonRecords` |
| 附加数据 | 同时批量获取用户最新头像/昵称 |
| 用户统计 | 总局数、总通关、最佳连胜 |

#### UI 展示

- 前三名：卡片式展示（第二名左侧、第一名中间放大、第三名右侧）
- 4-6 名：列表展示
- 不在前 6 名：显示"未上榜"

---

### 5.5 赛季系统

#### 赛季周期

- 每周六 00:00 ~ 次周五 24:00 为一个赛季
- 赛季编号格式：`YYYY-Sww`（如 `2025-S24`，基于年初第几周）
- 每周五 24:00 结算

#### 赛季数据字段

| 字段 | 说明 |
|------|------|
| `seasonScore` | 赛季最高分 |
| `seasonWave` | 赛季最高关卡 |
| `totalGames` | 赛季参与总局数 |
| `totalClears` | 赛季总通关次数 |
| `bestStreak` | 赛季最佳连胜 |

#### 赛季结算（settleSeason 云函数）

- 幂等设计（已结算不重复）
- 按分数和关卡分别排名 Top 100 归档到 `seasonArchive`
- 记录参与人数

#### 赛季变更检测

游戏启动时加载云端数据后，检测 `lastSeasonId` 是否变化，变化则触发上赛季结算。

---

### 5.6 用户授权系统

#### 双版本适配

| 微信版本 | 授权方式 |
|---------|---------|
| 低版本（SDK < 2.27） | 使用 `wx.getUserProfile` 弹出授权 |
| 高版本（SDK >= 2.27） | 使用 `wx.chooseAvatar` + `wx.showModal` 手动输入昵称 |

- 授权前先触发 `wx.requirePrivacyAuthorize` 隐私授权
- 默认昵称：未授权用户自动生成"泡泡大师XXXX"（4 位随机数）

---

## 六、数据库模型

### 6.1 gameData 集合（主用户数据）

```json
{
  "_openid": "string",
  "coins": "number",
  "highScore": "number",
  "bestWave": "number",
  "lastCheckinDate": "string (YYYY-MM-DD)",
  "checkinStreak": "number",
  "lastShareDate": "string (YYYY-MM-DD)",
  "todayShareCount": "number",
  "lastShareGiftDate": "string (YYYY-MM-DD)",
  "nickname": "string",
  "avatarUrl": "string",
  "seasonId": "string",
  "seasonScore": "number",
  "seasonWave": "number",
  "totalGames": "number",
  "totalClears": "number",
  "bestStreak": "number",
  "updatedAt": "Date"
}
```

### 6.2 seasonRecords 集合（赛季记录）

```json
{
  "_openid": "string",
  "seasonId": "string",
  "nickname": "string",
  "avatarUrl": "string",
  "highScore": "number",
  "bestWave": "number",
  "totalGames": "number",
  "totalClears": "number",
  "bestStreak": "number",
  "updatedAt": "Date"
}
```

### 6.3 seasonArchive 集合（赛季归档）

```json
{
  "seasonId": "string",
  "totalParticipants": "number",
  "topByScore": [
    {
      "rank": "number",
      "openid": "string",
      "nickname": "string",
      "avatarUrl": "string",
      "highScore": "number",
      "bestWave": "number",
      "totalGames": "number",
      "totalClears": "number",
      "bestStreak": "number"
    }
  ],
  "topByWave": [{ "..." }],
  "settledBy": "string",
  "settledAt": "Date"
}
```

---

## 七、云函数 API 端点

| 云函数名 | 功能 | 输入参数 | 返回数据 |
|---|---|---|---|
| `checkin` | 服务端签到 | 无（自动获取 openid） | `{ coins, checkinStreak, lastCheckinDate, reward }` |
| `saveGameData` | 保存游戏数据 | 白名单字段（coins, highScore 等） | `{ success, data }` |
| `loadGameData` | 加载游戏数据 | 无 | `{ success, data }` |
| `getLeaderboard` | 总排行榜 | `{ type: 'score'\|'wave' }` | `{ leaderboard[], userRank, userValue }` |
| `getSeasonLeaderboard` | 赛季排行榜 | `{ type: 'score'\|'wave' }` | `{ leaderboard[], userRank, userValue, userStats }` |
| `settleSeason` | 赛季结算 | `{ seasonId }` | `{ alreadySettled, totalParticipants }` |
| `getSeasonArchive` | 历史赛季归档 | `{ seasonId, type }` | `{ seasonId, leaderboard[] }` |

---

## 八、数据同步流程

```
游戏启动
  ↓
异步调用 loadCloudData 从云端加载
  ↓
本地与云端数据按策略合并
（高分取大值、金币取大值、签到以云端为准）
  ↓
检测赛季变更 → 触发上赛季结算
  ↓
游戏中只更新本地数据
  ↓
用户点击"返回首页"时 → 异步 saveToCloud 保存完整数据
```

---

## 九、配置常量汇总

| 配置项 | 值 | 说明 |
|---|---|---|
| `initialCoins` | 1000 | 新用户初始金币 |
| `maxLives` | 5 | 最大生命数 |
| `initialLives` | 5 | 初始生命数 |
| `maxPurchaseCount` | 3 | 每局最多购买生命次数 |
| `purchasePrices` | [300, 500, 1000] | 购买价格递增 |
| `maxShareCountPerDay` | 10 | 每日最大分享次数 |
| `maxShareReviveCount` | 3 | 每局最多分享复活次数 |
| `waveClear` | 5 | 1-19 关通关奖励 |
| `waveClearTier2` | 15 | 20-39 关通关奖励 |
| `waveClearTier3` | 30 | 40-59 关通关奖励 |
| `waveClearTier4` | 50 | 60+ 关通关奖励 |
| `share` | 50 | 快速分享奖励 |
| `shareGift` | 500 | 分享礼包奖励 |
| `consecutiveWin` | 5 | 连续胜利恢复生命间隔 |

---

## 十、性能优化策略

1. **离屏 Canvas 缓存**：背景、玻璃框网格、菜单 UI 均缓存到离屏 Canvas，减少每帧重绘
2. **批量绘制**：整个玻璃框网格合并为 1 次 `drawImage` 调用
3. **对象池**：GainNode 对象池复用（最多 10 个），减少音频高频触发时的 GC
4. **延迟创建渐变**：激活泡泡的渐变按需创建并缓存到泡泡对象上
5. **Storage 批量写入**：16ms 内合并多次写入为一次
6. **布局缓存**：按屏幕尺寸缓存布局计算结果
7. **数值宽度预计算**：0-9999 的文字宽度预先测量并缓存
8. **字体设置缓存**：避免每帧重复设置相同字体
9. **文字测量缓存**：LRU 策略（最多 200 条）
10. **帧时间限制**：`deltaTime` 上限 100ms，防止安卓大跳帧
11. **分阶段初始化**：先创建背景缓存启动游戏循环，再延迟创建玻璃框和 UI 缓存

---

## 十一、第三方集成

| 集成项 | 用途 |
|--------|------|
| 微信云开发 | 云函数、云数据库、云存储 |
| 微信社交 | 分享（`shareAppMessage`、`shareTimeline`）、隐私授权（`requirePrivacyAuthorize`）、用户信息（`getUserProfile`、`chooseAvatar`） |
| 微信游戏圈 | 通过 `wx.createPageManager()` 打开游戏圈 |
| Web Audio API | 音效合成（兼容微信 `wx.createWebAudioContext`） |

---

## 十二、兼容性处理

- 解决 iOS 微信小游戏中 `setTimeout`/`setInterval`/`requestAnimationFrame` 未暴露到 ES6 模块作用域的问题
- 多级别降级策略：`globalThis` -> `global` -> `wx` -> `setTimeout` 模拟 `requestAnimationFrame`
- 设置 `globalThis.__POLYFILL_LOADED__` 标记用于验证
