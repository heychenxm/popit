# 云函数调用优化方案

## 已实施的优化

### 优化 1：减少后台切换同步（预计减少 30-50% 调用）

**修改文件**：`src/Main.js`

**优化内容**：
- 增加同步间隔：1 分钟内不重复同步
- 后台切换时仍然强制同步（保证数据一致性）

### 优化 2：签到状态查询缓存（预计减少 20-30% 调用）

**修改文件**：`src/GameState.js`

**优化内容**：
- 添加 5 分钟缓存
- 云端和本地结果都缓存

### 优化 3：增加 CloudDataManager 防抖时间（预计减少 10-20% 调用）

**修改文件**：`src/CloudDataManager.js`

**优化内容**：
- 防抖时间从 5 秒增加到 10 秒
- 最小同步间隔从 3 秒增加到 5 秒

### 优化 4：增加排行榜缓存时间（预计减少 10-20% 调用）

**修改文件**：`src/GameState.js`

**优化内容**：
- 排行榜缓存从 5 分钟增加到 10 分钟

### 优化 5：合并云函数（减少 1 个云函数）

**修改文件**：
- `cloudfunctions/gameData/index.js` - 新增签到功能
- `src/GameState.js` - 更新签到调用

**优化内容**：
- 将 `checkin` 云函数合并到 `gameData` 云函数
- 新增 `checkinStatus` 和 `doCheckin` 操作
- 减少云函数数量，降低维护成本

**合并后的云函数支持的操作**：
- `sync` - 同步数据（游戏启动时）
- `update` - 更新数据（通关、分享等）
- `checkinStatus` - 获取签到状态
- `doCheckin` - 执行签到

### 优化 6：本地优先策略（减少云端依赖）

**修改文件**：`src/GameState.js`

**优化内容**：
- 优先使用本地数据
- 云端数据仅作为备份
- 只有当本地数据为空时，才使用云端数据
- 云端同步失败不影响游戏运行

**策略说明**：
- 最高分/最高关卡：本地优先，云端作为备份
- 金币数据：本地优先，云端作为备份
- 签到数据：本地优先，云端作为备份
- 分享礼包数据：本地优先，云端作为备份

## 优化效果预估

| 优化项 | 优化前 | 优化后 | 减少比例 |
|--------|--------|--------|----------|
| 后台切换同步 | 每次切换 1 次 | 1 分钟内不重复 | 30-50% |
| 签到状态查询 | 每次打开 1 次 | 5 分钟内不重复 | 20-30% |
| 通关数据同步 | 每次操作 1 次 | 10 秒内合并 | 10-20% |
| 排行榜查询 | 每次打开 1 次 | 10 分钟内不重复 | 10-20% |
| 云函数数量 | 5 个 | 4 个 | 减少 1 个 |
| 云端依赖 | 高 | 低 | 本地优先 |
| **综合优化** | - | - | **30-50%** |

## 进一步优化建议

### 1. 使用数据库直连（高级）

**建议**：
- 对于简单的数据读写，可以使用数据库直连
- 减少云函数调用次数

### 2. 实现数据版本控制

**建议**：
- 避免多设备数据冲突
- 实现数据合并策略

## 监控建议

1. **监控云函数调用次数**：
   - 在微信云开发控制台查看调用次数
   - 设置调用次数告警

2. **监控同步成功率**：
   - 记录同步成功/失败次数
   - 分析失败原因

3. **性能监控**：
   - 监控云函数响应时间
   - 优化慢查询

## 注意事项

1. **数据一致性**：
   - 本地数据优先，云端数据作为备份
   - 游戏启动时同步云端数据
   - 游戏退出时强制同步

2. **失败处理**：
   - 云函数调用失败不影响游戏运行
   - 失败后自动重试，最多 3 次

3. **用户体验**：
   - 云同步不影响游戏性能
   - 游戏退出时确保数据已同步

## 部署步骤

1. **上传更新后的 gameData 云函数**：
   - 在微信开发者工具中，右键点击 `cloudfunctions/gameData` 文件夹
   - 选择"上传并部署：云端安装依赖"

2. **测试验证**：
   - 编译项目
   - 测试签到功能
   - 测试数据同步功能

3. **可选：删除旧云函数**：
   - 确认新云函数工作正常后，可以删除 `checkin` 云函数
