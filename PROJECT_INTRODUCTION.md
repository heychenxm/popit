# 泡泡大师 - 项目详细介绍

## 项目概述

**泡泡大师**是一款基于微信平台的休闲益智类小游戏，以记忆力挑战为核心玩法，结合精美的霓虹视觉风格和丰富的社交系统，为玩家提供碎片化时间的脑力娱乐体验。

| 项目信息 | 说明 |
|---------|------|
| 项目名称 | 泡泡大师 |
| 项目代号 | popit |
| 运行平台 | 微信小游戏 |
| AppID | wxbd18a37b0c266cd0 |
| 游戏类型 | 休闲益智 / 记忆挑战 |
| 屏幕方向 | 竖屏（portrait） |
| 渲染技术 | Canvas 2D |
| 后端服务 | 微信云开发（环境：cloud1-d2gbhgc8abb1ab532） |
| 基础库版本 | 3.16.1 |
| 主语言 | 简体中文 |
| 商业模式 | IAA（无虚拟支付，无广告接入） |

---

## 核心玩法

### 游戏流程

```
主菜单(MENU) → 观察阶段(OBSERVE) → 点击阶段(PLAY) → 胜利(WIN) / 失败(FAIL)
```

### 玩法说明

1. **观察阶段（OBSERVE）**
   - 系统随机选取 N 个泡泡进行高亮闪烁（粉色/紫色/蓝色，按索引分配颜色）
   - 玩家需要在限定时间内记住这些泡泡的位置
   - 此阶段不可点击泡泡（点击会提示"先记住闪烁气泡的位置哦~"）
   - 观察时间随关卡提升而缩短

2. **点击阶段（PLAY）**
   - 观察结束后，所有泡泡恢复暗色常态
   - 玩家需要在倒计时内点击记忆中的目标泡泡
   - **点击正确**：泡泡变为亮色弹出，+5 分，播放 pop 音效和震动反馈
   - **点击错误**：泡泡短暂闪红，提示"点错了！"，不扣生命
   - **超时未完成**：扣除 1 点生命，还有生命则重试当前关，生命归零则游戏失败
   - **找齐全部目标**：关卡通关，获得金币奖励

3. **关卡进阶**
   - 游戏关卡无限推进，难度持续提升
   - 网格大小、目标数量、时间限制随关卡阶梯式变化

### 关卡难度曲线

| 关卡范围 | 网格大小 | 泡泡总数 | 目标数量 | 观察时间 | 点击时间 | 通关奖励 |
|---------|---------|---------|---------|---------|---------|---------|
| 1-19 关 | 4x4 | 16 | 2→7（每3关+1） | 2500ms→1500ms（每关-60ms） | 5000ms→3200ms（每关-100ms） | 15 金币 |
| 20-39 关 | 5x5 | 25 | 7→9（每5关+1） | 2200ms→1400ms（每关-40ms） | 4800ms→3400ms（每关-70ms） | 30 金币 |
| 40-59 关 | 6x6 | 36 | 9→11（每5关+1） | 2000ms→1400ms（每关-30ms） | 4500ms→3500ms（每关-50ms） | 50 金币 |
| 60+ 关 | 7x7 | 49 | 11→13（每5关+1） | 1800ms→1400ms（每关-20ms） | 4200ms→3600ms（每关-30ms） | 80 金币 |

### 生命系统

- 初始生命：3 点
- 最大生命：5 点
- 超时未完成关卡扣 1 点生命
- 连续 5 关胜利恢复 1 点生命（不超过上限）
- 生命归零时的续命方式：
  - **金币购买**：300 / 500 / 1000 金币（递增价格），每局最多 3 次
  - **分享复活**：分享到微信好友，每局最多 3 次

### 得分规则

- 每正确点击 1 个目标泡泡得 5 分
- 系统自动记录历史最高分（highScore）与最高通关关卡（bestWave）
- 破纪录时弹窗显示"新纪录!"徽章

---

## 经济系统

### 金币获取途径

| 途径 | 奖励 | 限制 |
|------|------|------|
| 初始赠送 | 1000 金币 | 新玩家 |
| 通关奖励 | 15/30/50/80 金币（按关卡阶梯） | 无限制 |
| 每日签到 | 300~3000 金币（按天数递增） | 每日 1 次 |
| 连续签到加成 | 第 7 天额外 +2000 | 需连续签到 |
| 快速分享 | +50 金币/次 | 每日上限 10 次 |
| 分享礼包 | +1000 金币 | 每日 1 次 |

### 金币消耗途径

| 用途 | 价格 | 限制 |
|------|------|------|
| 购买生命 | 300 / 500 / 1000 金币（递增） | 每局最多 3 次 |

> 金币仅为游戏内积分，不可充值、不可提现、不可兑换人民币。

---

## 技术架构

### 整体架构

```
game.js（入口文件 - polyfill + 云开发初始化 + 启动游戏）
  └── Main.js（主控制器 - 游戏循环、事件分发、流程管理）
        ├── GameState.js（状态管理 - 数据持久化、云同步、经济系统、赛季）
        ├── BubbleGrid.js（渲染引擎 - 泡泡网格 4x4~7x7、离屏缓存、动画）
        ├── UIManager.js（UI 系统 - 所有界面绘制、按钮交互、头像加载）
        ├── AudioManager.js（音效引擎 - Web Audio API 合成、对象池复用）
        ├── WechatAPI.js（微信 API 封装 - 云函数调用、社交功能）
        ├── config.js（数值配置 - 游戏参数、奖励规则）
        ├── seasonUtils.js（赛季工具 - 周期计算、倒计时）
        ├── utils.js（通用工具 - 颜色常量、几何计算、布局函数）
        ├── Icons.js（图标库 - Canvas 路径绘制所有图标）
        └── polyfill.js（兼容性补丁 - iOS 定时器/requestAnimationFrame）
```

### 云函数架构

```
cloudfunctions/
  ├── checkin/          # 服务端签到（防篡改）
  ├── saveGameData/     # 保存游戏数据（upsert + 赛季记录同步）
  ├── loadGameData/     # 加载游戏数据
  ├── getLeaderboard/   # 总排行榜（按分数/关卡排名）
  ├── getSeasonLeaderboard/  # 赛季排行榜
  ├── settleSeason/     # 赛季结算归档（幂等设计）
  └── getSeasonArchive/ # 查询历史赛季归档
```

### 云数据库集合

| 集合名 | 用途 | 主要字段 |
|--------|------|---------|
| gameData | 用户游戏数据 | _openid, coins, highScore, bestWave, nickname, avatarUrl, lastCheckinDate, checkinStreak, lastShareDate, todayShareCount, lastShareGiftDate |
| seasonRecords | 赛季参与记录 | _openid, seasonId, highScore, bestWave, totalGames, totalClears, bestStreak, nickname, avatarUrl |
| seasonArchive | 赛季归档（结算后） | seasonId, totalParticipants, topByScore, topByWave, settledAt |

### 数据同步策略

- **启动时**：异步从云端加载数据，与本地取较大值合并（不阻塞游戏启动）
- **游戏中**：数据实时保存到本地 Storage（批量延迟写入，16ms 合并）
- **返回主页时**：异步保存到云端（不阻塞用户）
- **排行榜/赛季排名**：30 分钟本地缓存，过期后从云端刷新
- **签到**：优先云端校验（防篡改），云端不可用时降级到本地

### 性能优化

- **离屏 Canvas 缓存**：背景、玻璃框网格、主菜单等静态内容缓存到离屏 Canvas，减少每帧 draw call
- **批量绘制**：玻璃框网格一次性复制到主 Canvas（1 次 draw call）
- **渐变对象复用**：预创建并缓存渐变对象，避免每帧创建
- **GainNode 对象池**：音效复用 GainNode，减少 GC 压力
- **字体/文字测量缓存**：缓存字体设置和 measureText 结果
- **数字宽度预计算**：0-9999 的数值宽度预先计算
- **DeltaTime 限幅**：最大 100ms，防止安卓端跳帧
- **延迟初始化**：使用 requestIdleCallback 延迟创建非关键缓存

---

## 模块详解

### 1. game.js - 入口文件

- 加载 polyfill（iOS 兼容性补丁）
- 初始化微信云开发
- 注册全局错误处理（onError / onUnhandledRejection）
- 创建 Main 实例启动游戏
- 导出 game 实例供调试

### 2. Main.js - 主控制器

核心职责：
- **游戏循环**：requestAnimationFrame 驱动，update → render 循环
- **事件分发**：根据当前 phase 和 screen 分发触摸事件
- **流程管理**：开始游戏、关卡切换、暂停/恢复、胜利/失败处理
- **分享系统**：快速分享、分享礼包、分享复活
- **授权流程**：隐私授权 → 获取昵称头像（兼容高低版本微信）
- **排行榜/赛季排名**：数据加载、类型切换
- **签到**：云端签到 + 降级本地
- **游戏圈**：通过 PageManager 打开游戏圈

### 3. GameState.js - 状态管理

核心职责：
- **游戏状态**：分数、关卡、生命、连击、阶段（phase）
- **数据持久化**：批量读写 Storage，延迟合并写入
- **云端同步**：启动加载、返回主页保存、数据合并策略
- **经济系统**：金币、购买生命、分享复活、签到奖励
- **赛季系统**：赛季信息初始化、赛季数据累积、赛季变更结算
- **排行榜**：云端 + 本地缓存获取

### 4. BubbleGrid.js - 渲染引擎

核心职责：
- **动态网格**：支持 4x4 到 7x7 网格，根据关卡自动切换
- **泡泡渲染**：暗色常态 / 发光激活态 / 错误红色态
- **玻璃框效果**：圆角矩形玻璃质感边框
- **离屏缓存**：背景、玻璃框网格缓存到离屏 Canvas
- **动画系统**：发光相位、缩放脉动、星星闪烁
- **点击检测**：根据触摸坐标返回泡泡索引
- **配色方案**：16 个基础配色 + 动态生成额外配色（最多 49 个）

### 5. UIManager.js - UI 系统

管理的界面：
- **主菜单**：Logo、最高分、授权按钮、开始按钮、底部功能按钮、赛季横幅、分享礼包图标
- **游戏 HUD**：暂停按钮、关卡进度、得分/最高分/生命卡片、阶段提示、倒计时进度条
- **胜利弹窗**：星星装饰、奖励展示、最高分（破纪录徽章）、返回首页/下一关
- **失败弹窗**：哭泣泡泡图标、得分、金币购买续命、分享复活、重新开始
- **暂停弹窗**：继续游戏 / 返回首页
- **排行榜弹窗**：前三名卡片 + 列表、最高分/最高关卡切换、骨架屏
- **赛季排名弹窗**：同排行榜结构、赛季编号显示、底部结算提示
- **签到弹窗**：7 天网格、连签奖励卡片、签到按钮
- **分享礼包弹窗**：消息预览、发送按钮
- **Toast 提示**：自动消失的浮动提示

### 6. AudioManager.js - 音效引擎

- 基于 Web Audio API 合成（无音频文件依赖）
- 4 种音效：pop（泡泡爆破）、wrong（错误）、success（成功音阶）、click（点击）
- GainNode 对象池复用，减少 GC 压力
- 兼容微信 WebAudioContext 和浏览器 AudioContext
- 支持声音开关

### 7. WechatAPI.js - 微信 API 封装

- 通用 wx API Promise 封装
- 云函数调用封装（callCloud）
- 提供：saveGameData、loadGameData、cloudCheckin、getLeaderboard、getSeasonLeaderboard、settleSeason、getSeasonArchive
- 社交功能：分享菜单、分享消息
- 系统功能：屏幕常亮、震动反馈

---

## 功能系统

### 1. 签到系统

- **触发方式**：点击底部签到按钮
- **签到规则**：
  - 每日 1 次，连续签到累计天数，中断后重新计算
  - 奖励按天数递增：第1天300、第2天500、第3-6天1000、第7天3000（含 bonus +2000）
  - 7 天为一个周期，循环往复
- **数据处理**：优先调用云端签到（服务端校验日期和奖励，防篡改），云端不可用时降级本地

### 2. 分享系统

- **快速分享**（底部分享按钮）：
  - 调用 wx.shareAppMessage 分享到微信好友
  - 奖励 +50 金币，每日上限 10 次
  - 冷却机制：3 秒内不可重复触发

- **分享礼包**（右上角宝箱图标）：
  - 弹出分享礼包弹窗
  - 奖励 +1000 金币，每日 1 次
  - 包含微信消息预览展示

- **分享复活**（失败弹窗中）：
  - 分享到微信好友后返回游戏，恢复 1 点生命
  - 每局最多 3 次
  - 通过 isWaitingShareRevive 标志 + onShow 回调检测分享返回

### 3. 赛季系统

- **赛季周期**：每周六 00:00 至次周五 24:00
- **赛季编号**：YYYY-Sww 格式（如 2025-S24，基于 ISO 周数）
- **统计数据**：赛季最高分、最高关卡、通关次数、最佳连胜、参与局数
- **赛季排名**：独立的赛季排行榜，支持按分数/关卡切换
- **赛季结算**：
  - 检测到赛季变更时自动触发上赛季结算
  - 结算将排行榜 Top 100 归档到 seasonArchive 集合
  - 幂等设计：已结算赛季不会重复结算
- **展示**：主菜单赛季横幅（含倒计时）、赛季排名弹窗

### 4. 用户系统

- **默认昵称**：未授权时生成"泡泡大师XXXX"随机昵称
- **授权流程**：
  - 点击授权按钮 → 隐私授权（wx.requirePrivacyAuthorize）→ 获取昵称头像
  - 低版本微信（< 2.27）：使用 wx.getUserProfile 弹窗授权
  - 高版本微信（>= 2.27）：引导用户手动选择头像（wx.chooseAvatar）+ 输入昵称
- **数据存储**：本地 Storage + 云端 gameData 集合
- **头像显示**：支持图片头像加载 + 文字头像降级 + 默认头像兜底

### 5. 排行榜系统

- **总排行榜**：基于 gameData 集合，按 highScore 或 bestWave 降序
- **赛季排行榜**：基于 seasonRecords 集合，按当季数据降序
- **排名规则**：相同分数同名次，支持并列排名
- **展示**：前三名卡片式展示（第1名居中突出），后续列表展示
- **缓存**：30 分钟本地缓存，减少云端请求
- **骨架屏**：数据加载中显示闪烁骨架屏动画

### 6. 游戏圈

- 通过 wx.createPageManager().load() 打开
- 使用 config 中配置的 openlink 链接
- 排行榜弹窗和赛季排名弹窗中均有入口按钮

---

## 界面说明

### 1. 主菜单界面

- 顶部左侧：金币余额徽章（金币图标 + 数值）
- 中央：泡泡大师 Logo 图片（自适应屏幕宽度）
- Logo 下方：最高关卡 + 最高分
- 中部：授权按钮（未授权时显示，渐变色）
- 中部："开始游戏"按钮（金色 3D 风格，带阴影和高光）
- 底部：4 个圆形功能按钮（排行榜、声音、签到、分享）+ 标签 + 通知红点
- 底部上方：赛季活动横幅（皇冠图标 + 倒计时 + 查看详情按钮）
- 右上角：分享礼包宝箱图标（每日可领时显示，带弹跳动画）

### 2. 游戏界面

- 左上：暂停按钮（圆形背景 + 两条竖线）
- 顶部中央：当前关卡数 + 进度点（每 5 关一个进度点，共 4 个）
- HUD 卡片：得分 / 历史最高 / 生命（心形图标）
- 阶段提示区："请观察！记住闪烁的气泡" 或 "点它！"
- 泡泡网格区域（玻璃框容器包裹）
- 底部：倒计时进度条（时钟图标 + 进度条 + 剩余时间文字）

### 3. 弹窗界面

- **胜利弹窗**：红色标题 + 三颗星星 + 奖励展示 + 最高分（破纪录徽章） + 返回首页/下一关
- **失败弹窗**：灰色标题 + 哭泣泡泡图标 + 得分 + 金币购买/分享复活按钮 + 返回首页
- **暂停弹窗**：暂停图标 + 返回首页/继续游戏

---

## 视觉设计

### 色彩方案

- **背景**：深紫星空渐变（#0d0926 → #17113a → #2b185d）
- **泡泡暗色态**：每个泡泡有独特的 center/edge/dark 三色渐变
- **泡泡激活态**：粉色（#ec4899）、紫色（#c084fc）、蓝色（#3b82f6）
- **按钮**：金色渐变（#ffd13b → #ff9e00）+ 白色边框 + 3D 阴影
- **UI 元素**：半透明深色背景（rgba）+ 彩色边框

### 动画效果

- 泡泡闪动：观察阶段目标泡泡的 shadowBlur 和 scale 周期性变化（sin 波）
- 点击后固定：scale=1.04，shadowBlur=25
- 星星粒子：背景 15 个星星闪烁（sin 波透明度变化）
- 霓虹网格：底部网格背景装饰
- 玻璃框：圆角矩形玻璃质感（渐变填充 + 高光叠加）
- 分享礼包图标：上下弹跳动画
- Toast 提示：淡入上滑 → 停留 → 淡出下滑

### 音效系统

所有音效通过 Web Audio API 实时合成，无外部音频文件：

| 音效 | 波形 | 频率变化 | 时长 |
|------|------|---------|------|
| pop（泡泡爆破） | 正弦波 | 400Hz → 1200Hz | 150ms |
| wrong（错误） | 锯齿波 | 180Hz → 100Hz | 450ms |
| success（成功） | 三角波 | C5-E5-G5-C6 上升音阶 | 4x300ms |
| click（点击） | 正弦波 | 600Hz → 300Hz | 60ms |

---

## 配置说明

### config.js

```javascript
game: {
  initialCoins: 1000,              // 初始金币
  maxLives: 5,                     // 最大生命
  initialLives: 3,                 // 初始生命
  maxPurchaseCount: 3,             // 每局最大购买生命次数
  purchasePrices: [300, 500, 1000], // 递增购买价格
  maxShareCountPerDay: 10,         // 每日最大分享次数
  maxShareReviveCount: 3,          // 每局最大分享复活次数
}

checkin: {
  rewards: { 1: { base: 300 }, 2: { base: 500 } },
  defaultBase: 500,                // 默认基础奖励
  bonusDay: 7,                     // bonus 周期（7 的倍数天）
  bonusAmount: 1000,               // bonus 额外奖励
}

rewards: {
  waveClear: 15,                   // 1-19 关通关奖励
  waveClearTier2: 30,              // 20-39 关通关奖励
  waveClearTier3: 50,              // 40-59 关通关奖励
  waveClearTier4: 80,              // 60+ 关通关奖励
  share: 50,                       // 快速分享奖励
  shareGift: 1000,                 // 分享礼包奖励
  consecutiveWin: 5,               // 连续胜利恢复生命间隔
}

gameClub: {
  openlink: '...',                 // 游戏圈 openlink
  enabled: true,
}
```

---

## 项目文件结构

```
popit/
├── game.js                          # 游戏入口（polyfill + 云初始化 + 启动）
├── game.json                        # 游戏配置（竖屏、网络超时）
├── project.config.json              # 项目配置（AppID、编译选项、云函数路径）
├── project.private.config.json      # 项目私有配置
├── .gitignore
├── LICENSE                          # MIT 许可证
│
├── src/                             # 前端源代码
│   ├── Main.js                      # 主控制器（游戏循环、事件、流程）
│   ├── GameState.js                 # 状态管理（数据、经济、赛季、云同步）
│   ├── BubbleGrid.js                # 渲染引擎（泡泡网格、离屏缓存、动画）
│   ├── UIManager.js                 # UI 系统（所有界面绘制和交互）
│   ├── AudioManager.js              # 音效引擎（Web Audio API 合成）
│   ├── WechatAPI.js                 # 微信 API 封装（云函数、社交）
│   ├── config.js                    # 数值配置
│   ├── seasonUtils.js               # 赛季周期计算工具
│   ├── utils.js                     # 通用工具（颜色、几何、布局、存储）
│   ├── Icons.js                     # Canvas 图标绘制库
│   ├── polyfill.js                  # iOS 兼容性补丁
│   └── assets/
│       └── images/
│           ├── pop_logo.png         # 游戏 Logo
│           └── temp-avatar.png      # 默认头像
│
└── cloudfunctions/                  # 云函数
    ├── checkin/                     # 服务端签到（防篡改）
    │   ├── index.js
    │   └── package.json
    ├── saveGameData/                # 保存游戏数据 + 赛季记录
    │   ├── index.js
    │   └── package.json
    ├── loadGameData/                # 加载游戏数据
    │   ├── index.js
    │   └── package.json
    ├── getLeaderboard/              # 总排行榜
    │   ├── index.js
    │   └── package.json
    ├── getSeasonLeaderboard/        # 赛季排行榜
    │   ├── index.js
    │   └── package.json
    ├── settleSeason/                # 赛季结算归档
    │   ├── index.js
    │   └── package.json
    └── getSeasonArchive/            # 查询历史赛季归档
        ├── index.js
        └── package.json
```

---

## 开发相关

### 开发环境

- 微信开发者工具
- ES6+ 语法（项目配置启用 ES6 转换、postcss、压缩）
- 云开发环境 ID：cloud1-d2gbhgc8abb1ab532

### 关键特性

- 响应式布局（适配不同屏幕尺寸和 DPI）
- 高 DPI 支持（canvas 物理像素适配 + ctx.scale 逻辑像素映射）
- 云端数据同步（启动合并、返回保存、防篡改签到）
- 离屏 Canvas 缓存优化（背景、玻璃框、UI 缓存）
- 音效对象池复用（减少 GC 压力）
- iOS 兼容性补丁（定时器、requestAnimationFrame）
- 全局错误处理（onError + onUnhandledRejection）
- 生命周期管理（前后台切换、分享复活检测）
- 骨架屏加载动画
- 分享冷却防刷机制（3 秒冷却 + 每日上限）

---

## 内容合规

| 维度 | 说明 |
|------|------|
| 题材 | 抽象泡泡记忆挑战，无剧情 |
| 暴力血腥 | 无 |
| 恐怖元素 | 无 |
| 不良诱导 | 无 |
| 社交功能 | 仅排行榜展示 + 游戏圈，无聊天/交友 |
| 支付系统 | 无人民币充值，无虚拟支付 |
| 广告 | 当前版本未接入 |
| 用户隐私 | 仅获取昵称/头像，用于排行榜展示 |
| 适龄建议 | 8+ 或 12+ |
| 备案名称 | 泡泡大师 |

---

## 版本状态

| 功能模块 | 状态 |
|---------|------|
| 核心玩法（记忆泡泡挑战） | 已完成 |
| 关卡系统（无限关卡，4 阶难度递增） | 已完成 |
| 生命系统 + 金币购买续命 | 已完成 |
| 分享复活续命 | 已完成 |
| 金币经济系统（阶梯式奖励） | 已完成 |
| 云端数据同步（gameData 集合） | 已完成 |
| 总排行榜（云端 + 缓存） | 已完成 |
| 赛季系统（周期、排名、结算归档） | 已完成 |
| 签到系统（云端校验 + 本地降级） | 已完成 |
| 分享系统（快速分享 + 礼包 + 冷却防刷） | 已完成 |
| 音效与震动反馈（Web Audio 合成） | 已完成 |
| 用户系统（授权 + 高低版本兼容） | 已完成 |
| 游戏圈入口 | 已完成 |
| 离屏 Canvas 性能优化 | 已完成 |
| iOS 兼容性补丁 | 已完成 |
| 微信广告接入 | 未接入 |

---

*文档更新时间：2026-06-22*
