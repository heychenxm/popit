# 排行榜功能实现说明

## 功能概述

实现了全服排行榜功能，支持查看**最高分排行榜**和**最高关卡排行榜**，并可以在两种类型之间切换。

---

## 已实现的功能

### 1. 云函数 `getLeaderboard`
**位置**: `cloudfunctions/getLeaderboard/index.js`

**功能**:
- 获取全服排行榜数据
- 支持按最高分 (`score`) 或最高关卡 (`wave`) 排序
- 返回前 6 名数据 + 用户自己的排名
- 智能处理用户排名：
  - 用户在前 6 名内：显示前 6 名
  - 用户不在前 6 名：显示前 5 名 + 用户自己的排名
  - 超过 100 名显示 "100+"

**调用示例**:
```javascript
wx.cloud.callFunction({
  name: 'getLeaderboard',
  data: { type: 'score' }  // 'score' 或 'wave'
})
```

---

### 2. GameState 扩展
**位置**: `src/GameState.js`

**新增方法**:
```javascript
async getLeaderboard(type = 'score')
```

**功能**: 调用云函数获取排行榜数据，处理成功/失败情况

---

### 3. UIManager 扩展
**位置**: `src/UIManager.js`

**新增状态**:
```javascript
this.leaderboardType = 'score'  // 'score' | 'wave'
this.leaderboardData = null     // 排行榜数据
```

**新增方法**:

1. **`drawLeaderboardModal(gameState)`**
   - 绘制排行榜弹窗主界面
   - 包含标题、切换按钮、前三名展示区、列表区
   - 支持关闭按钮

2. **`drawLeaderboardRankCard(...)`**
   - 绘制前三名卡片
   - 第 1 名：金色边框 + 放大显示 + 🏆图标
   - 第 2 名：银色边框
   - 第 3 名：铜色边框

3. **`drawLeaderboardListItem(...)`**
   - 绘制排行榜列表项（第 4-6 名 + 自己）
   - 自己的排名：紫色边框高亮
   - 前 3 名：金色文字高亮

4. **`drawAvatar(...)`** 和 **`drawTextAvatar(...)`**
   - 绘制用户头像
   - 支持文字头像（显示 openid 前 3 位）
   - 第 1 名：金色渐变背景
   - 其他人：紫色渐变背景

---

### 4. Main 扩展
**位置**: `src/Main.js`

**新增方法**:

1. **`async showLeaderboard()`**
   - 打开排行榜弹窗
   - 默认显示最高分排行榜
   - 自动刷新数据

2. **`async refreshLeaderboard()`**
   - 从云端获取最新排行榜数据
   - 更新 UI 显示

3. **`async switchLeaderboardType(type)`**
   - 切换排行榜类型（最高分/最高关卡）
   - 自动刷新数据

4. **`closeLeaderboard()`**
   - 关闭排行榜弹窗
   - 返回主菜单

5. **`handleLeaderboardTouch(x, y)`**
   - 处理排行榜界面的触摸事件
   - 支持关闭、切换类型

---

## UI 设计细节

### 弹窗布局
```
┌─────────────────────────────┐
│  ✕                          │  关闭按钮
│     🏆 排行榜                │  标题
│  ┌──────────┬──────────┐    │  切换按钮
│  │ 最高分   │ 最高关卡 │    │
│  └──────────┴──────────┘    │
├─────────────────────────────┤
│  ┌────┐ ┌────┐ ┌────┐      │  前三名
│  │ 2  │ │ 1  │ │ 3  │      │  （第 1 名放大）
│  └────┘ └────┘ └────┘      │
├─────────────────────────────┤
│  4. 头像 昵称         2340  │  列表项
│  5. 头像 昵称         1980  │  （自己高亮）
│  6. 头像 昵称         1540  │
│  100+. 头像 昵称       890  │  （模糊排名）
├─────────────────────────────┤
│  新赛季将于每周五 24:00...  │  底部提示
└─────────────────────────────┘
```

### 颜色方案
- **第 1 名**: 🥇 金色 (`#fbbf24` - `#d97706`)
- **第 2 名**: 🥈 银色 (`#94a3b8`)
- **第 3 名**: 🥉 铜色 (`#eab308`)
- **自己**: 紫色高亮 (`#6366f1`)
- **背景**: 靛蓝渐变 (`#312e81` → `#4c1d95`)

---

## 使用流程

### 1. 打开排行榜
用户点击主菜单的"排行榜"按钮 → 调用 `showLeaderboard()` → 显示弹窗并加载数据

### 2. 切换类型
用户点击"最高关卡"按钮 → 调用 `switchLeaderboardType('wave')` → 刷新数据

### 3. 关闭排行榜
用户点击右上角"✕"按钮 → 调用 `closeLeaderboard()` → 返回主菜单

---

## 数据规则

### 排名计算
- 按指定字段（`highestScore` 或 `highestWave`）降序排序
- 分数/关卡相同时，按 `lastUpdateTime` 降序排序（更新时间近的在前）

### 显示规则
- **前 3 名**: 特殊卡片展示，第 1 名放大
- **第 4-6 名**: 列表展示
- **自己的排名**: 
  - 在前 6 名内：不重复显示
  - 不在前 6 名：额外显示在列表末尾
  - 超过 100 名：显示 "100+"

### 头像规则
- **已授权**: 使用真实头像 URL（TODO: 需要实现图片加载）
- **未授权**: 使用 openid 前 3 位作为文字头像

---

## 待完善功能

### 1. 头像图片加载
当前 `drawAvatar` 方法中 TODO 部分需要实现图片加载功能：
```javascript
// TODO: 加载并绘制微信头像图片
const img = wx.createImage()
img.src = avatarUrl
img.onload = () => {
  ctx.drawImage(img, x - size/2, y - size/2, size, size)
}
```

### 2. 用户昵称
当前使用 `openid` 生成默认昵称，未来可以：
- 在用户首次登录时获取微信昵称
- 存储到 `user_profile` 集合
- 支持用户自定义昵称

### 3. 数据更新时机
当前只在打开排行榜时更新数据，未来可以考虑：
- 关卡结束时异步更新云端数据
- 使用云函数批量更新
- 添加防抖机制避免频繁调用

---

## 云函数部署

### 1. 上传云函数
在微信开发者工具中：
1. 右键点击 `cloudfunctions/getLeaderboard` 目录
2. 选择"上传并部署：云端安装依赖"

### 2. 测试云函数
在微信开发者工具控制台测试：
```javascript
wx.cloud.callFunction({
  name: 'getLeaderboard',
  data: { type: 'score' }
}).then(res => {
  console.log(res)
})
```

### 3. 数据库权限
确保 `user_profile` 集合的权限设置允许：
- 所有用户可读
- 仅创建者可写
- 云函数可读写

---

## 技术亮点

1. ✅ **智能排名显示**: 自动判断用户是否在前 6 名，避免重复显示
2. ✅ **模糊排名**: 超过 100 名显示 "100+"，保护用户隐私
3. ✅ **视觉层次**: 前三名特殊展示，第 1 名突出显示
4. ✅ **交互流畅**: 切换类型自动刷新，无需关闭重开
5. ✅ **头像降级**: 未授权时使用文字头像，避免空白
6. ✅ **性能优化**: 只在需要时获取数据，减少云函数调用

---

## 相关文件清单

```
popit/
├── cloudfunctions/
│   └── getLeaderboard/
│       ├── index.js          # 云函数主逻辑
│       └── package.json      # 依赖配置
├── src/
│   ├── GameState.js          # 添加 getLeaderboard 方法
│   ├── UIManager.js          # 添加排行榜 UI 绘制
│   └── Main.js               # 添加排行榜交互逻辑
└── LEADERBOARD_IMPLEMENTATION.md  # 本文档
```

---

## 下一步建议

1. **测试云函数**: 在微信开发者工具中测试云函数功能
2. **完善头像**: 实现微信头像图片加载
3. **添加动画**: 为排行榜弹窗添加打开/关闭动画
4. **优化性能**: 考虑添加数据缓存机制
5. **扩展功能**: 可以添加好友排行榜、金币排行榜等

---

## 常见问题

### Q: 云函数调用失败？
A: 检查云环境 ID 是否正确，云函数是否已上传部署

### Q: 排行榜数据为空？
A: 检查 `user_profile` 集合是否有数据，权限设置是否正确

### Q: 头像显示异常？
A: 当前使用文字头像，图片头像功能待实现

### Q: 排名计算不准确？
A: 检查云函数中的排序逻辑和排名计算逻辑

---

**实现完成时间**: 2026-06-04  
**版本**: v1.0.0
