# 用户信息授权弹窗修复说明

## 问题原因

微信小游戏规定 `wx.getUserProfile` **不能在页面加载时自动弹出**，必须由用户**主动触发**（如点击按钮）才能调用。

之前的代码在游戏启动时自动调用 `initUserInfo()`，违反了微信的规定，导致授权弹窗无法显示。

---

## 修复方案

### 修改后的流程

```
游戏启动
  ↓
正常运行（不弹出授权）
  ↓
用户点击"排行榜"按钮
  ↓
检查是否已授权
  ↓
未授权 → 显示授权提示对话框
  ↓
用户点击"立即授权"
  ↓
弹出微信官方授权窗口
  ↓
用户同意 → 获取真实头像昵称
  ↓
用户拒绝 → 使用默认头像
```

---

## 已修改的代码

### 1. Main.js - `showLeaderboard()` 方法

**新增逻辑**:
```javascript
async showLeaderboard() {
  // ...
  
  // 检查用户是否授权，未授权则提示
  if (!this.gameState.userInfo.authorized) {
    const confirmed = await this.showUserInfoAuthDialog()
    if (confirmed) {
      await this.initUserInfo()
    }
  }
  
  // 获取排行榜数据
  await this.refreshLeaderboard()
}
```

### 2. Main.js - `showUserInfoAuthDialog()` 方法

**新增方法**:
```javascript
async showUserInfoAuthDialog() {
  return new Promise((resolve) => {
    wx.showModal({
      title: '用户信息授权',
      content: '授权后将在排行榜中显示您的头像和昵称，是否立即授权？',
      confirmText: '立即授权',
      cancelText: '暂不',
      success: (res) => {
        resolve(res.confirm)
      },
      fail: () => {
        resolve(false)
      }
    })
  })
}
```

### 3. Main.js - `init()` 方法

**移除**:
```javascript
// 移除游戏启动时的自动调用
// this.initUserInfo().then(() => { ... })
```

---

## 使用流程

### 第一次打开排行榜

1. 点击主菜单"排行榜"按钮
2. 弹出提示对话框：
   - 标题：用户信息授权
   - 内容：授权后将在排行榜中显示您的头像和昵称，是否立即授权？
   - 按钮：立即授权 / 暂不

3. **点击"立即授权"**:
   - 弹出微信官方授权窗口
   - 显示用户的头像和昵称预览
   - 点击"允许" → 获取真实信息 → 保存到云端

4. **点击"暂不"**:
   - 使用默认头像（Canvas 绘制）
   - 默认昵称（玩家 + SDKVersion）
   - 排行榜正常显示

### 再次打开排行榜

- 已授权：直接显示排行榜，使用真实头像昵称
- 未授权：直接显示排行榜，使用默认头像

---

## 测试步骤

### 步骤 1: 清除本地数据（测试用）
在微信开发者工具中：
```
工具 → 清除数据 → 清除全部
```

### 步骤 2: 启动游戏
- 游戏启动，**不会**弹出授权窗口 ✅

### 步骤 3: 点击排行榜按钮
- 弹出授权提示对话框 ✅

### 步骤 4: 点击"立即授权"
- 弹出微信官方授权窗口 ✅
- 显示头像和昵称预览 ✅

### 步骤 5: 点击"允许"
- 获取真实头像昵称 ✅
- 保存到云端 ✅
- 排行榜显示真实信息 ✅

### 步骤 6: 重新打开游戏
- 不会再次弹出授权窗口 ✅
- 直接使用已授权的信息 ✅

---

## 授权失败处理

### 场景 1: 用户点击"暂不"
- 使用默认头像（Canvas 绘制）
- 默认昵称：`玩家 + SDKVersion`
- 排行榜正常显示

### 场景 2: 用户点击授权窗口"拒绝"
- 捕获异常
- 使用默认头像
- 保存到云端（标记为未授权）

### 场景 3: 网络错误
- 云端保存失败
- 本地保存信息
- 下次有机会再同步

---

## 优势

### 1. 符合微信规范
- ✅ 用户主动触发
- ✅ 明确告知用途
- ✅ 不强制授权

### 2. 用户体验好
- ✅ 二次确认，避免误操作
- ✅ 拒绝也能正常使用
- ✅ 默认头像美观

### 3. 性能优化
- ✅ 只授权一次
- ✅ 本地缓存
- ✅ 异步处理

---

## 注意事项

### 1. 授权时机
- ❌ 游戏启动时（违规，会被拒绝）
- ✅ 用户点击按钮后（合规）

### 2. 授权说明
- ✅ 清晰明确："用于完善用户资料，在排行榜中展示"
- ❌ 模糊不清："用于提供更好的服务"

### 3. 拒绝处理
- ✅ 优雅降级：使用默认头像
- ❌ 强制阻止：不允许使用游戏

---

## 相关文件

- `src/Main.js` - 主要逻辑修改
- `src/GameState.js` - 用户信息管理
- `src/UIManager.js` - 头像绘制

---

## 后续优化建议

### 1. 添加"我的"页面
- 用户中心
- 查看/修改头像昵称
- 授权管理

### 2. 授权刷新机制
- 定期提醒更新头像
- 检测微信头像变化
- 手动刷新按钮

### 3. 隐私保护
- 授权记录可查
- 支持取消授权
- 数据删除功能

---

**修复完成时间**: 2026-06-04  
**版本**: v1.0.1
