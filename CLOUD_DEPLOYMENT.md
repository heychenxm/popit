# 微信云开发部署说明

## 一、云函数部署步骤

### 1. 开通云开发
1. 打开微信开发者工具
2. 点击顶部工具栏的"云开发"按钮
3. 开通云开发服务（选择免费版即可）
4. 记录下环境 ID（格式类似：cloud1-xxx）

### 2. 配置云环境 ID
打开 `src/Main.js` 文件，找到第 18 行左右：
```javascript
wx.cloud.init({
  env: 'your-env-id', // TODO: 替换为实际的云环境 ID
  traceUser: true
})
```
将 `'your-env-id'` 替换为你实际的云环境 ID。

### 3. 上传云函数
在微信开发者工具中：
1. 右键点击 `cloudfunctions/syncData` 目录
2. 选择"上传并部署：云端安装依赖"
3. 等待上传完成
4. 同样步骤上传 `checkin` 和 `updateGameData` 云函数

### 4. 创建数据库集合
在云开发控制台：
1. 点击"数据库"
2. 点击"添加集合"
3. 创建集合 `user_profile`（用户档案）
4. 创建集合 `user_signin`（签到数据）

### 5. 设置数据库权限
为了安全，建议设置以下权限：
- `user_profile`: 所有用户可读写（云函数会自动处理权限）
- `user_signin`: 所有用户可读写（云函数会自动处理权限）

## 二、功能说明

### 1. 数据同步（syncData 云函数）
- **功能**：游戏启动时同步本地和云端数据
- **策略**：采用"最大值优先"原则
- **同步数据**：
  - 最高关卡
  - 最高分
  - 金币
  - 签到数据

### 2. 签到功能（checkin 云函数）
- **功能**：处理每日签到
- **特点**：
  - 云端严格验证
  - 云端不可用时禁止签到
  - 使用事务保证原子性
  - 7 天循环奖励（100/200/5 宝石/500/1000/10 宝石/2000 金币）

### 3. 更新游戏数据（updateGameData 云函数）
- **功能**：更新最高关卡、最高分、金币等
- **特点**：
  - 使用事务保证原子性
  - 支持绝对值更新和相对值增加
  - 自动取最大值

## 三、降级策略

### 云端不可用时的处理
1. **签到功能**：禁止签到，显示"网络错误，请稍后重试"
2. **数据同步**：使用本地数据，不影响游戏进行
3. **游戏数据更新**：先保存到本地，网络恢复后手动同步

### 数据冲突处理
- 采用"最大值优先"原则
- 本地和云端对比后取最大值
- 确保玩家进度不会丢失

## 四、测试检查清单

### 云函数测试
- [ ] 云函数是否上传成功
- [ ] 云函数日志是否正常
- [ ] 数据库集合是否创建
- [ ] 数据库权限是否正确

### 功能测试
- [ ] 新用户签到是否正常
- [ ] 连续签到是否正常
- [ ] 重复签到是否被阻止
- [ ] 云端数据同步是否正常
- [ ] 最高分保存是否正常
- [ ] 金币增加是否正常

### 异常测试
- [ ] 云端不可用时签到是否被禁止
- [ ] 网络超时是否有提示
- [ ] 本地数据是否正常保存

## 五、常见问题

### Q1: 云函数调用失败
**解决方法**：
1. 检查云环境 ID 是否正确
2. 检查云函数是否上传成功
3. 查看云函数日志排查错误

### Q2: 数据库操作失败
**解决方法**：
1. 检查数据库集合是否创建
2. 检查数据库权限设置
3. 查看控制台错误信息

### Q3: 签到功能不可用
**解决方法**：
1. 检查 checkin 云函数是否正常
2. 查看云函数日志
3. 确认云端是否可访问

## 六、后续优化建议

1. **数据迁移**：编写脚本将现有本地数据迁移到云端
2. **排行榜功能**：利用云端数据实现好友排行榜
3. **推送提醒**：实现签到时间提醒功能
4. **补签功能**：消耗宝石补签遗漏的天数
5. **数据分析**：统计玩家行为数据

## 七、安全注意事项

1. **云函数权限**：确保云函数权限设置正确
2. **数据验证**：云函数中已做服务器端验证
3. **防作弊**：云端严格验证签到日期
4. **隐私保护**：只存储必要的游戏数据

---

**部署完成后，请进行全面测试确保功能正常！**
