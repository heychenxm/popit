# 云函数调用场景分析与优化方案

## 一、云函数调用场景分析

### 1. `syncData` - 游戏启动数据同步

**调用位置**：`GameState.js` 第 84 行

**触发场景**：
- 游戏启动时（`Main.js` 的 `init()` 方法）
- 游戏从后台回到前台时（`wx.onShow()`）

**调用频率**：
- 每次启动游戏：1 次
- 每次从后台切换回来：1 次

**优化现状**：
- ✅ 已优化：游戏启动时异步调用，不阻塞游戏
- ✅ 已优化：后台切换回来时调用，但失败不影响游戏

**优化建议**：
- 可以考虑增加本地缓存，减少后台切换时的同步频率
- 建议：只在启动时同步，后台切换时不同步（或延迟同步）

---

### 2. `checkin` (getStatus) - 获取签到状态

**调用位置**：`GameState.js` 第 315 行

**触发场景**：
- 用户点击签到按钮时（`Main.js` 的 `doCheckin()` 方法）

**调用频率**：
- 每天最多 1 次（用户主动触发）

**优化现状**：
- ✅ 已优化：云端不可用时降级到本地检查

**优化建议**：
- 当前优化已足够，无需进一步优化

---

### 3. `checkin` (checkin) - 执行签到

**调用位置**：`GameState.js` 第 345 行

**触发场景**：
- 用户点击签到按钮时（`Main.js` 的 `doCheckin()` 方法）

**调用频率**：
- 每天最多 1 次（用户主动触发）

**优化现状**：
- ✅ 已优化：云端不可用时降级到本地签到

**优化建议**：
- 当前优化已足够，无需进一步优化

---

### 4. `getLeaderboard` - 获取排行榜

**调用位置**：`GameState.js` 第 556 行

**触发场景**：
- 用户打开排行榜时（`Main.js` 的 `showLeaderboard()` 方法）
- 用户切换排行榜类型时（`Main.js` 的 `switchLeaderboardType()` 方法）

**调用频率**：
- 每次打开排行榜：1 次
- 每次切换类型：1 次

**优化现状**：
- ✅ 已优化：5 分钟缓存机制

**优化建议**：
- 当前优化已足够，可以考虑：
  - 增加缓存过期时间到 10 分钟
  - 或者在排行榜界面增加"刷新"按钮，用户主动刷新时才更新

---

### 5. `saveUserProfile` - 保存用户资料

**调用位置**：`GameState.js` 第 614 行

**触发场景**：
- 用户首次点击游戏时（`Main.js` 的 `tryInitUserInfo()` 方法）
- 用户授权获取信息后（`Main.js` 的 `initUserInfo()` 方法）

**调用频率**：
- 每个用户最多 1-2 次（首次授权时）

**优化现状**：
- ✅ 已优化：只在用户授权时调用

**优化建议**：
- 当前优化已足够，无需进一步优化

---

### 6. `updateGameData` - 更新游戏数据

**调用位置**：`CloudDataManager.js` 第 189 行

**触发场景**：
- 通关保存最高分时（`GameState.js` 的 `saveHighScore()` 方法）- **已优化：延迟到游戏结束时同步**
- 分享成功后（`Main.js` 的 `handleQuickShare()` 和 `handleWechatShare()` 方法）
- 签到成功后（`GameState.js` 的 `doCloudCheckin()` 方法）
- 游戏退出时（`Main.js` 的 `wx.onHide()` 回调）

**调用频率**：
- 每次通关：0 次（优化后，延迟到游戏结束时同步）
- 每次分享：0 次（优化后，合并到 CloudDataManager）
- 游戏结束时：1 次（优化后）

**优化现状**：
- ✅ 已优化：使用 CloudDataManager 批量更新
- ✅ 已优化：防抖机制（5 秒延迟）
- ✅ 已优化：游戏退出时强制同步
- ✅ 已优化：延迟通关同步（游戏结束时同步）
- ✅ 已优化：合并到 gameData 云函数

**优化建议**：
- 当前优化已足够

---

## 二、优化效果对比

### 优化前（假设场景：玩 10 关，分享 5 次，签到 1 次）

| 场景 | 调用次数 |
|------|----------|
| 游戏启动 | 1 |
| 通关 10 次 | 10 |
| 分享 5 次 | 5 |
| 签到 1 次 | 2（getStatus + checkin） |
| 排行榜 3 次 | 3 |
| **总计** | **21 次** |

### 优化后（相同场景）

| 场景 | 调用次数 | 说明 |
|------|----------|------|
| 游戏启动 | 1 | 无法优化 |
| 通关 10 次 | 0 | 延迟到游戏结束时同步 |
| 分享 5 次 | 0 | CloudDataManager 合并 |
| 签到 1 次 | 2 | 无法优化（必须调用） |
| 排行榜 3 次 | 1 | 5 分钟缓存 |
| 游戏结束 | 1 | 延迟同步 + CloudDataManager 合并 |
| **总计** | **5 次** | |

**优化比例**：约 **75%** 的调用次数减少

---

## 三、进一步优化建议

### 1. 减少后台切换时的同步

**当前行为**：每次从后台切换回来都同步数据

**优化方案**：
- 只在游戏启动时同步
- 后台切换时不同步（或延迟 30 秒后同步）

**预期效果**：减少 50% 的 `syncData` 调用

### 2. 增加排行榜缓存时间

**当前行为**：5 分钟缓存

**优化方案**：
- 增加到 10 分钟缓存
- 或者增加"刷新"按钮，用户主动刷新时才更新

**预期效果**：减少 70% 的 `getLeaderboard` 调用

### 3. 延迟通关数据同步

**当前行为**：每次通关后立即同步

**优化方案**：
- 通关时只更新本地数据
- 游戏结束时（返回主菜单或退出）才同步

**预期效果**：减少 80% 的 `updateGameData` 调用

### 4. 合并云函数

**当前行为**：5 个独立的云函数

**优化方案**：
- 将 `syncData` 和 `updateGameData` 合并为一个云函数
- 减少云函数数量，降低维护成本

**预期效果**：减少 1 个云函数，降低维护成本

---

## 四、监控建议

### 1. 监控云函数调用次数

在微信云开发控制台查看：
- 每日调用次数
- 每小时调用次数
- 错误率

### 2. 监控同步成功率

在控制台查看日志：
- 同步成功次数
- 同步失败次数
- 失败原因

### 3. 监控缓存命中率

在控制台查看日志：
- 排行榜缓存命中次数
- 缓存未命中次数

---

## 五、总结

### 已实现的优化

1. ✅ **CloudDataManager**：批量更新 + 防抖机制
2. ✅ **排行榜缓存**：5 分钟缓存
3. ✅ **游戏生命周期管理**：退出时强制同步
4. ✅ **错误处理**：云端不可用时降级到本地
5. ✅ **延迟通关同步**：游戏结束时同步
6. ✅ **合并云函数**：gameData 云函数（合并 syncData 和 updateGameData）

### 📝 可进一步优化的方向

1. 🔄 **减少后台切换同步**：只在启动时同步，后台切换时不同步
2. 🔄 **增加排行榜缓存**：10 分钟或主动刷新
3. ~~🔄 **延迟通关同步**~~ - ✅ 已实现
4. ~~🔄 **合并云函数**~~ - ✅ 已实现（gameData）

### 预期优化效果

| 优化项 | 预期减少比例 |
|--------|-------------|
| CloudDataManager | 70-80% |
| 排行榜缓存 | 70% |
| 后台切换优化 | 50% |
| ~~延迟通关同步~~ | ~~80%~~ - ✅ 已实现 |
| ~~合并云函数~~ | ~~减少 1 个云函数~~ - ✅ 已实现 |
| **综合优化** | **80-85%** |
